@model List<Secondary_School_Result_Management_System.Models.Entities.Subject>
@{
    ViewData["Title"] = "Subjects";
}

@if (TempData["Success"] != null)
{
    <div class="alert-edu-success">
        <i class="bi bi-check-circle-fill"></i> @TempData["Success"]
    </div>
}

<div class="section-header">
    <div>
        <div class="section-title">Subjects</div>
        <div class="section-sub">Manage school subjects</div>
    </div>
    <a href="/Admin/CreateSubject" class="btn-accent">
        <i class="bi bi-plus-lg"></i> Add Subject
    </a>
</div>

<div class="edu-card">
    @if (Model == null || !Model.Any())
    {
        <div style="text-align:center; padding:48px 0; color:var(--text-muted);">
            <i class="bi bi-book" style="font-size:48px; display:block; margin-bottom:16px;"></i>
            <div style="font-size:15px; font-weight:600;">No subjects yet</div>
            <div style="font-size:13px; margin-top:4px;">Add your first subject to get started</div>
        </div>
    }
    else
    {
        <table class="edu-table">
            <thead>
                <tr>
                    <th>Subject Name</th>
                    <th style="text-align:right;">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var subject in Model)
                {
                    <tr>
                        <td style="font-weight:600;">
                            <i class="bi bi-book-fill" style="color:var(--accent); margin-right:8px;"></i>
                            @subject.Name
                        </td>
                        <td style="text-align:right;">
                            <form asp-action="DeleteSubject" asp-controller="Admin" method="post"
                                  style="display:inline;"
                                  onsubmit="return confirm('Delete @subject.Name?')">
                                <input type="hidden" name="id" value="@subject.Id" />
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn-danger-sm">
                                    <i class="bi bi-trash3"></i> Delete
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>
